type constants =
  {
  windowHeight: int;
  windowWidth: int;
  memberLength: int;
  memberWidth: int;
  tileSize: int;}
let constantsState =
  {
    windowHeight = 256;
    windowWidth = 512;
    memberLength = 5;
    memberWidth = 3;
    tileSize = 32
  }
