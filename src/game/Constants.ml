type constants =
  {
  windowHeight: int;
  windowWidth: int;
  memberLength: int;
  memberWidth: int;
  tileSize: int;}
let constantsState =
  {
    windowWidth = 1536;
    windowHeight = 768;
    memberLength = 5;
    memberWidth = 10;
    tileSize = (32 * 3)
  }
