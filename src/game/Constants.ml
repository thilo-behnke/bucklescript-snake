type constants =
  {
  windowHeight: int;
  windowWidth: int;
  memberLength: int;
  memberWidth: int;
  tileSize: int;}
let constantsState =
  {
    windowWidth = 800;
    windowHeight = 600;
    memberLength = 5;
    memberWidth = 10;
    tileSize = (20)
  }
